org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Proje (E:) ile Gradle/pub onbellegi (C:) farkli surucude oldugunda Kotlin'in
# incremental derleme onbellegi "this and base files have different roots"
# hatasiyla cokuyor (bilinen Windows-ozel Kotlin/Gradle sorunu). Incremental
# derleme kapatilarak calisir hale getirildi; derleme biraz daha yavas ama
# guvenilir olur.
kotlin.incremental=false
kotlin.incremental.android=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
